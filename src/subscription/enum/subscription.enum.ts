export enum TransactionStatus {
	PENDING = 'pending',
	SUCCESS = 'success',
	FAILED = 'failed',
	CANCELED = 'canceled',
}

export enum PaymentMethod {
	MOMO = 'momo',
	VISA = 'visa',
	ZALOPAY = 'zalopay',
	BANK_TRANSFER = 'bank_transfer',
}
